@layout('main')
@section('content')
    <a href="/posts">Go Back</a>
    <h1>{{ post.title }}</h1>
    <p>{{ post.body }}</p>
    <hr>
    <a class="btn btn-dark d-block float-left" href="/posts/edit/{{ post.id }}">Edit</a>
    <form action="{{ '/posts/' + post.id + '?_method=DELETE' }}" method="POST">
        {{ csrfField() }}
        <button type="submit" class="btn btn-danger float-right">Delete</button>
    </form>
@endsection